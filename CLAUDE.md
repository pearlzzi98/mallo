## 세션 재개 (/rsm)

사용자가 `/rsm` 이라고 하면 즉시 아래 절차를 수행하라.

1. CLAUDE.md와 TODO.md를 읽어 컨텍스트를 복원한다.
2. 복원 후 사용자에게 다음 항목을 요약하여 전달한다.
   - 프로젝트 현재 상태
   - 마지막으로 완료된 작업
   - 진행 중이거나 미완료 항목 (있는 경우)
   - 이어서 할 작업 확인

---

## /cpt 실행 전 필수 작업

사용자가 `/cpt` 라고 하면 즉시 아래 절차를 수행하라.

1. CLAUDE.md와 TODO.md를 현재 작업 내용 기준으로 최신 상태로 업데이트한다.
2. `/compact`를 진행한다.
3. 변경사항이 있으면 `git commit`을 수행한다.

---

## 세션 종료 (/ex)

사용자가 `/ex` 라고 하면 즉시 아래 절차를 수행하라.

1. `/cpt` 절차를 전체 수행한다.
2. 사용자에게 다음 항목을 요약하여 전달한다.
   - 오늘 완료한 작업
   - 미완료 항목 (있는 경우)

---

## Git 커밋 컨벤션

커밋 메시지는 **타입 prefix(영어) + 본문(한국어)** 형식을 따른다.

### 형식
```
<type>: <변경 내용 요약 (한국어)>
```

### 타입 목록
| 타입 | 용도 |
|------|------|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 변경 (README, CLAUDE.md 등) |
| `style` | 코드 포맷, 디자인 변경 (기능 변경 없음) |
| `refactor` | 리팩토링 (기능/버그 변경 없음) |
| `chore` | 빌드, 설정 파일 변경 (.gitignore 등) |

### 커밋/푸쉬 실행 트리거

| 트리거 | 동작 |
|--------|------|
| "commit", "커밋" | `git commit`만 수행 |
| "push", "푸쉬" | `git push`만 수행 |
| "commit&push", "커푸" | `git commit` + `git push` 순서대로 수행 |

**"커푸" 수행 시 절차:**
1. `git status`와 `git diff`로 변경 사항을 확인한다.
2. 민감한 파일 여부를 판단해 진행한다.
   - **민감한 파일이 없는 경우**: 확인 없이 즉시 실행
   - **민감한 파일이 포함된 경우**: 사용자에게 먼저 알리고 확인 후 진행

**민감한 파일 분류 기준:**
- `.env`, API 키, 시크릿 등 보안 정보가 담긴 파일
- `.gitignore`에 등록된 파일 또는 디렉토리 및 하위 파일
- 프로젝트 소유자 전용 파일로 공개 저장소에 올라가서는 안 되는 파일

> ⚠️ gitignore에 등록된 파일은 커밋/푸시 대상에서 자동 제외되지만,
> 해당 파일의 **내용을 응답에 포함하거나 공개 채널에 노출하지 않도록** 주의한다.

---

## 보안 주의사항

- `.env` 파일은 절대 커밋하지 않는다.
- 보안상 민감한 파일이 포함될 경우 사용자에게 먼저 확인 후 커밋한다.
